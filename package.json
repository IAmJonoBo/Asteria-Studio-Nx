{
  "name": "asteria-studio",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm -w apps/asteria-desktop run lint",
    "test": "npm -w apps/asteria-desktop run test",
    "build": "npm -w apps/asteria-desktop run build"
  }
}
